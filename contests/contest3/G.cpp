#include <bits/stdc++.h>

using namespace std;

int main()
{ /*
     long long n, a, b, soma = 1;

     cin >> n;

     while (n--)
     {
         cin >> a >> b;

         for (int i = a; i > 0; i--)
         {
             if (b % i == 0)
                 b = b / i;
         }

         cout << "A: " << a << "B: " << b << endl;

         while (soma <= b)
         {
             soma = a * b * soma;
         }
         cout << soma << endl;
         soma = 1;
     }

     return 0;

 */
}